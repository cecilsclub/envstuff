name: Test

on:
  push:
    branches:
      - develop
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    environment: development

    steps:
      - run: |
          echo ${{ vars.SECURITY_GROUP }}

